# cora / GAT / transductive
# Hyperparameters as published in the paper (Appendix B, Table 6).
extends: _base.yaml
dataset: cora
teacher: GAT
exp_setting: tran

num_layers: 3
hidden_dim: 1024
dropout_t: 0.3
dropout_s: 0.6
learning_rate: 0.01

# --- InfGraND core hyperparameters ---
lamb: 0.5
tau: 1.2
delta_1: 0.4
delta_2: 0.2
gamma_1: 1.0
gamma_2: 0.1

# --- feature propagation (paper Eq. 5, 7) ---
propagation_hops: 2
