name: infgrand
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      - torch==1.6.0
      - dgl==0.6.1          # CPU build; for NVIDIA GPUs use dgl-cu111==0.6.1 instead
      - numpy>=1.19,<2
      - scipy>=1.5
      - pyyaml>=5.4
